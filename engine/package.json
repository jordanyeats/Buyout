{
  "name": "buyout-engine",
  "version": "0.1.0",
  "description": "Deterministic, framework-agnostic game engine for Buyout",
  "type": "module",
  "main": "src/index.ts",
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "devDependencies": { "typescript": "^5.5.0", "vitest": "^2.0.0" }
}
