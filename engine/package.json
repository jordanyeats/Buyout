{
  "name": "buyout-engine",
  "version": "0.1.0",
  "description": "Test suite and typecheck gate for the Buyout game engine (source: mobile/src/engine)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
